CMAKE_MINIMUM_REQUIRED(VERSION 3.10)
PROJECT(LipSync3rdparty)

FILE(MAKE_DIRECTORY ${PROJECT_INCLUDE_DIR})
FILE(COPY spdlog DESTINATION ${PROJECT_INCLUDE_DIR})
FILE(COPY nlohmann DESTINATION ${PROJECT_INCLUDE_DIR})
